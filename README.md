# Description
My first react-native app written with pure react-native.

# How to Setup
**1:** git clone :

**2:** cd to cloned repo:

**3:** install with `yarn` or `npm i`

**4** for iOS `react-native run-ios` and for Android, `react-native run-android`

